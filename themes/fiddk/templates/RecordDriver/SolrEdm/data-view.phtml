<? foreach ($data as $key => $field): ?>
    <? foreach ($field as $resource => $view): ?>
    <? if ($view != '') :?>
      <a href="<?=$resource?>" target="_blank"><?=$this->escapeHtml($view)?></a>
    <? endif; ?>
    <? endforeach;?>
  <br/>
<? endforeach; ?>
